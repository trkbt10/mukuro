import {
  "moonbitlang/core/json",
  "moonbitlang/core/encoding/utf8" @utf8,
}

warnings = "-1"

options(
  link: { "native": { "stub-cc-link-flags": "-lz" } },
  "native-stub": [ "deflate_stubs.c" ],
)
