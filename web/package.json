{
  "name": "mukuro-web",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "workspaces": ["client", "dashboard"],
  "scripts": {
    "dev": "bun run --cwd dashboard dev",
    "start": "bun run --cwd dashboard start",
    "build": "bun run --cwd client build && bun run --cwd dashboard build",
    "test": "bun run --cwd client test && bun run --cwd dashboard test",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@playwright/test": "^1.58.2",
    "eslint": "^10.0.2",
    "globals": "^17.4.0",
    "typescript-eslint": "^8.56.1"
  }
}
