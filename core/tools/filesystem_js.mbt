///|
/// ファイルシステムツール - JS実装（未サポート）

///|
fn fs_read(_path : String) -> ToolExecutionResult {
  ToolExecutionResult::error(
    "Filesystem operations are not supported in JavaScript environment",
  )
}

///|
fn fs_write(_path : String, _content : String) -> ToolExecutionResult {
  ToolExecutionResult::error(
    "Filesystem operations are not supported in JavaScript environment",
  )
}

///|
fn fs_append(_path : String, _content : String) -> ToolExecutionResult {
  ToolExecutionResult::error(
    "Filesystem operations are not supported in JavaScript environment",
  )
}

///|
fn fs_delete(_path : String, _recursive : Bool) -> ToolExecutionResult {
  ToolExecutionResult::error(
    "Filesystem operations are not supported in JavaScript environment",
  )
}

///|
fn fs_exists(_path : String) -> ToolExecutionResult {
  ToolExecutionResult::error(
    "Filesystem operations are not supported in JavaScript environment",
  )
}

///|
fn fs_list(_path : String) -> ToolExecutionResult {
  ToolExecutionResult::error(
    "Filesystem operations are not supported in JavaScript environment",
  )
}

///|
fn fs_mkdir(_path : String) -> ToolExecutionResult {
  ToolExecutionResult::error(
    "Filesystem operations are not supported in JavaScript environment",
  )
}

///|
fn fs_stat(_path : String) -> ToolExecutionResult {
  ToolExecutionResult::error(
    "Filesystem operations are not supported in JavaScript environment",
  )
}
